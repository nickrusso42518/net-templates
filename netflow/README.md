# Flexible NetFlow (FNF) Templates
Apply to interface using:

```
ip flow monitor FLOW_MONITOR_IPV4 [input|output]
ipv6 flow monitor FLOW_MONITOR_IPV6 [input|output]
mpls flow monitor FLOW_MONITOR_MPLS [input|output]
```
